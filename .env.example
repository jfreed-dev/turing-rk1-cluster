# Turing Pi BMC Configuration
# Copy this file to .env and fill in your credentials
#
# TPI CLI connection settings for BMC access
TPI_HOSTNAME=10.10.88.70
TPI_USERNAME=root
TPI_PASSWORD=your-password-here

# Use local tpi CLI instead of SSH to BMC for deploy-talos-cluster.sh
# Set to 1 to use local tpi, 0 or unset to use SSH
USE_LOCAL_TPI=1

# Optional overrides (defaults shown)
# BMC_IP=10.10.88.70
# SSH_USER=root
